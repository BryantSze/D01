{% extends 'base.html.j2' %}

{% block app_content %}
<h1>{{ title }}</h1>
<form method="post" action="{{ url_for('search') }}">
    {{ form.csrf_token }}
    {{ form.userID.label }} {{ form.userID(size=32) }}
    <button type="submit">{{ _('Search') }}</button>
</form>
{% endblock %}